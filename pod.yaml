apiVersion: v1
kind: Pod
metadata:
  name: api-pod
  labels:
    run: connectApi
spec:
  containers:
    - name: client
      image: rodrigoruas/kubernete-python-sample
      ports:
        - containerPort: 3000